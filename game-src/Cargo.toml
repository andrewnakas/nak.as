[workspace]
resolver = "2"
members = ["crates/sim", "crates/protocol", "crates/wasm"]

[workspace.package]
edition = "2021"
version = "0.1.0"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
